# Frontend environment defaults
# Copy this file to `.env.local` or `.env` before running the Vite dev server.

# Security gateway base URL (sanitize/upload proxy)
VITE_SECURITY_GATE_URL=http://localhost:8000
# Orchestrator API for login + job handling
VITE_ORCHESTRATOR_URL=http://localhost:8008
